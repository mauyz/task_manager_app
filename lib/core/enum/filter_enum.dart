/// Enum representing the different task filters available
enum FilterEnum {
  /// Filter to show all tasks, regardless of their completion status.
  all,

  /// Filter to show only completed tasks.
  completed,

  /// Filter to show only uncompleted tasks.
  uncompleted,
}
